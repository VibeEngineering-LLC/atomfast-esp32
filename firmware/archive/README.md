# Архив прошивок atomfast-esp32

Снимки прошлых версий, на случай отката. Каждая подпапка — копия
`firmware/atomfast_gateway.yaml` ровно перед заменой на следующую версию.

Семантика версий и подробное описание изменений — в основном
[`../CHANGELOG.md`](../CHANGELOG.md). Здесь — только короткие пометки
«что и почему» для быстрой навигации.

| Версия | Папка | Дата | Когда применять / почему отказались |
|---|---|---|---|
| v0.6.0 | [v0.6.0/](v0.6.0/) | 2026-06-12 | Первая стабильная BLE-сборка: scan 1100/350 ms, passive scan, INFO-логгер, счётчики реконнектов. **Минус**: засорял лог warning'ом `[W][ble_sensor:095]: handle 37 status=2` раз в минуту (косметика, BLE при этом работал). |
| v0.6.1 | [v0.6.1/](v0.6.1/) | 2026-06-12 | Подавлен `handle 37` warning через игнор статуса в notify-callback'е. BLE-стабильность не пострадала. |
| v0.6.2 | [v0.6.2/](v0.6.2/) | 2026-06-12 | Косметика веб-UI (sort weights, иконки). Минусов нет. |
| v0.7.0 | [v0.7.0/](v0.7.0/) | 2026-06-13 | Первая попытка миграции на esp-idf. Регрессия по реконнектам (`rsn 0x3e × 2`, CONNECTED #7) — **INC-09**. Откат на arduino через v0.7.2. |
| v0.7.2 | [v0.7.2/](v0.7.2/) | 2026-06-13 | Возврат на arduino-framework как стабильный baseline после INC-09. 40-минутный прогон без `rsn 0x3e`. |
| v0.7.12 | [v0.7.12/](v0.7.12/) | 2026-06-14 | Финальная arduino-сборка перед `v0.8.0-step4`: подобраны scan-окна `640 / 32` (5 % duty), снижение `rsn 0x8` (выборка малая — 2 события / 30 мин против 5 / 10 мин на v0.7.2, экстраполяция «в ~7×» приближённая). |
| v0.7.12-webv2-alt | [v0.7.12-webv2-alt/](v0.7.12-webv2-alt/) | 2026-06-14 | Альтернативный Web UI v2 (без spark-graph, lite-режим). Не зашло — оставлен снимок на случай, если на DevKitC v3 потребует слишком много heap'а. |

Версии до v0.6.0 (v0.5.x — первый рабочий вариант, до BLE-стабильности) —
смотри в git-истории по commit-хешам:

- **v0.5.0** (skill creation): commit `c89a438` — первая публичная прошивка.
- **Народмон-выгрузка**: commit `a6d2e21` — добавлен 5-min HTTP POST.
- **v0.6.0**: commit `44b2239` — BLE-стабильность (scan 32 % duty, passive).

Чтобы достать любой YAML из git:

```bash
git show <commit>:atomfast-esp32/firmware/atomfast_gateway.yaml > old.yaml
```

## Когда обновлять архив

При каждой выкладке новой стабильной прошивки в `firmware/atomfast_gateway.yaml`:

1. Создать `archive/<old-version>/atomfast_gateway.yaml` (копия старого файла).
2. Добавить строку в таблицу выше — версия, папка, дата, одна фраза почему отказались.
3. Тогда уже коммитить и пушить новую версию.

**Архив только для классической прошивки.** S3-baseline (`atomfast_gateway_s3.yaml`,
v0.9.0-s3 и далее) пока не архивируется — это вторая параллельная ветка,
снимки старых версий хранятся в git-истории.
